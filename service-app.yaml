apiVersion: v1
kind: Service
metadata:
  name: chatbot-abacate-svc
  labels:
    app: chatbot-abacate
spec:
  type: ClusterIP
  selector:
    app: chatbot-abacate
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
